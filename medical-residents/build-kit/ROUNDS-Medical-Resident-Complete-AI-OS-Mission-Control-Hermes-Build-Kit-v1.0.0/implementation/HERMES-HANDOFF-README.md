
# Hermes Handoff README

Read the root implementation program and resolved functional-build prompt. Treat `source/` as immutable and work only in the owner-approved copy-on-write path.

Before every phase, reverify sources, authorization, profile, primary/secondary hats, record scope, partition, ATTEND, data ceiling, Hermes capabilities, migrations, processes, and rollback. Do not carry approval across changed content, context, source, model, tool, recipient, destination, or version.

Checkpoint receipts are build evidence, never app records, and contain no sensitive payloads. Reconcile 424/424 results to the matrix and ledger. No Not Run or Failed result permits either ready state. `Core operational; AI setup pending` additionally permits Blocked only for `CTL-AI-002..007` and `INT-044`, solely after the controlled-unconfigured path passes and solely because a configured backend is absent. Any other Blocked row means Not operational.
