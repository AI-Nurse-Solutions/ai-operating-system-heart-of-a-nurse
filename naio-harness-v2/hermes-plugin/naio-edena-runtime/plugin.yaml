name: naio-edena-runtime
version: "2.0.0-shadow.1"
description: "NAIO EDENA runtime observer. Shadow mode only: records metadata-only would-allow/block/approve decisions and never changes Hermes tool behavior."
author: "NAIO Institute"
hooks:
  - pre_tool_call
requires_env:
  - NAIO_HARNESS_ROOT
