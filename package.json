{
  "name": "nurse-ai-os-static-site-tests",
  "private": true,
  "scripts": {
    "test": "npm run test:switchboard-schema && npm run test:switchboard-browser && npm run test:student-nurse-browser && python3 -m unittest discover -s tests -p 'test_*.py' -v",
    "test:switchboard-browser": "node tests/test_switchboard_browser.mjs",
    "test:switchboard-schema": "node tests/test_switchboard_schema.mjs",
    "test:student-nurse-browser": "node tests/test_student_nurse_browser.mjs"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "playwright-core": "1.61.1"
  }
}
