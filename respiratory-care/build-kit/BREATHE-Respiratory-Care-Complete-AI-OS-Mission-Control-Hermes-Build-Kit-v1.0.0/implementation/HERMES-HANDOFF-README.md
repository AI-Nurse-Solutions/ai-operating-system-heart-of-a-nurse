
# Hermes Handoff README

Read the root `GIVE-THIS-PACKAGE-TO-HERMES.md` and the resolved functional-build prompt. Treat `source/` as immutable. Work only in the owner-approved copy-on-write path.

Before each phase, reverify source hashes, authorization, active task hat/workspace/partition, data and device ceilings, five SCOPE gates, Hermes capability state, migrations, running processes and rollback point. Do not carry a prior approval across a changed context, payload, source, model, tool, destination or version.

Checkpoint receipts are durable build evidence, not application records and never contain sensitive payloads. Reconcile every result to the matrix and ledger. No `Not Run`, failed critical or unresolved core record may be called Operational.
