
# Baseline Gap Report

The baseline supplies useful interface and mission-loop reference material, but a source review cannot establish the Respiratory Care Professional target as operational. Hermes must verify and, where missing, implement authentication, durable server persistence, migrations, closed data admission, context isolation, Respiratory Care Professional authority gates, exact BREATHE catalogs, consent memory, evidence/source records, agent permissions, genuine provider streaming/cancellation, diagnostics, backup/restore/update/rollback/removal/uninstall and executable tests.

The Respiratory Care Professional source packages contain written programs, not a downloadable functional app. They contain no canonical route, product ID, target app version, machine schemas, agent registry or four-level badge system. Those are explicitly build-layer additions.

The standalone BREATHE directory and ZIP differ in README/manifest bytes, and the directory manifest references setup guides absent from that directory. Preserve both; do not “repair” the immutable sources. The later integrated Complete Edition controls the current one-package installation order.
